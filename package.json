{
  "name": "truco-venezolano-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:frontend": "npm --prefix client run dev",
    "dev:backend": "npm --prefix server run dev",
    "dev:lan": "powershell -ExecutionPolicy Bypass -File scripts/dev-lan.ps1"
  }
}
